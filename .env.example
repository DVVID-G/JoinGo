# Example environment variables for NovaMeet backend
PORT=3000

# Option 1: Individual Firebase credential pieces
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk@example.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nABC...XYZ\n-----END PRIVATE KEY-----\n"

# Option 2 (preferred): Absolute path to downloaded service account JSON
# When set, the JSON file will be used instead of the individual vars above.
FIREBASE_CREDENTIALS_JSON_PATH=C:\\absolute\\path\\to\\service-account.json

# STUN server configuration (later sprints)
STUN_SERVER_URL=stun:your-stun-host:3478

# AI Summarization (optional)
OPENAI_API_KEY=replace-if-needed

# Firebase Web/API key (required for server-side email/password sign-in)
# Obtain from Firebase Console -> Project Settings -> Web API Key
FIREBASE_API_KEY=AIzaSyD7qwKi9WvLs3aaD_Lgsib3uc98v_dw5OU

# Optional - SMTP for sending reset emails
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=

# OAuth server-side credentials (if you implement server-side code-exchange flow)
# Obtain these from provider developer consoles and set them in the environment.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
FACEBOOK_APP_ID=
FACEBOOK_APP_SECRET=
